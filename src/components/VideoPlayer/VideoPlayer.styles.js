import styled from 'styled-components';

export const VideoContainer = styled.div`
  width: 100%;
  height: 100%;
  background: #000;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;

  .bmpui-ui-container {
    width: 100%;
    height: 100%;
  }

  .bmpui-ui-controlbar {
    background: rgba(0, 0, 0, 0.7);
  }

  .bmpui-ui-controlbar-topbar {
    background: rgba(0, 0, 0, 0.7);
  }
`; 